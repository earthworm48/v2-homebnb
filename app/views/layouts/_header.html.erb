<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag("logo.jpg", height:50), root_path, class:"pull-left"%>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <%= form_for Listing.new, method: :get, :html => {:class => "navbar-form navbar-left"}, :url => search_path, role: "search" do |f| %>
        <%= text_field_tag :term, params[:term],placeholder:"Put the location here" %>
        <%= f.submit 'search', :class => 'btn btn-default' %>
      <% end %>

      <ul class="nav navbar-nav navbar-right">


      <% if current_user %>
      <li>

          <%= link_to image_tag(current_user.image_url, class:"img-circle",id:"user-pic"), current_user , class:"img-circle-a"%>

      </li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= current_user.email%> <span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">
          <li><%= link_to "Add Listing", new_listing_path%></li>
          <li><%= link_to "All Listing", listings_path %></li>
          <li><%= link_to "My Profile", current_user %></li>

          <li><%= link_to 'Sign out', sign_out_path, method: :delete %></li>
        </ul>
      </li>
      <%else%>
        <li><%=link_to 'Sign Up', '#sign_up_modal', "data-toggle" => 'modal'%></li>
        <li><%=link_to 'Login', '#log_in_modal', "data-toggle" =>'modal' %></li>     
      <%end%>
</nav>